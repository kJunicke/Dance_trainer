- Added loading spinners for every blocking Supabase wait (board list/detail loads, create/join/import board, login/signup, dev quick-login) — optimistic card/column mutations intentionally left without spinners
- Fixed cards being nearly unclickable when they have no description (`min-height` on `.task-card`, since the inline-editable title otherwise ate almost the whole clickable area)
- Trello import was failing on label insert (400) — traced to the `labels.color` check constraint never having been updated for the aubergine/brass rename, despite a migration for it already sitting on `main` — [[Supabase]]
	- The GitHub → Supabase auto-deploy silently didn't apply that migration; found and fixed live via the `supabase` Claude Code plugin (MCP) rather than waiting on the integration
	- Migration itself had a bug too: dropped the old constraint after backfilling data instead of before, so the backfill got rejected by the constraint it was replacing
