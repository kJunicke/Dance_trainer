- [feature] Per-block inline editing for card notes — clicking a block swaps only that block for an editor, the rest stays rendered — [[Card Notes]]
- [decision] Rejected Obsidian-style live preview (CodeMirror 6 decorations, ~1–2 weeks, weakest on touch) — the complaint was losing your place in the whole-field mode swap, not seeing `**` markers. Per-block editing solves that directly — [[Card Notes]]
- [decision] Block granularity is the `marked` lexer's, so a whole bullet list opens as one editor. Shipped as-is to see whether it actually grates before splitting `list_item`s — [[Card Notes]]
- [fix] Block editor auto-grows instead of scrolling internally — raw markdown is taller than its rendered form, so a fixed-height editor hid most of what you were editing — [[Card Notes]]
- [fix] Escape in a block editor no longer closes the entire card modal — [[Card Notes]]
- [fix] Clicking a second block while one was open opened the wrong block or none — the commit reflows the note between mousedown and click. Resolved on mousedown now, carrying the target as a source offset across the reflow — [[Card Notes]]
- [fix] Block editor closed itself the instant it opened — Vue flushes nextTick before mousedown's default focus action, so the backdrop stole focus and blur-saved. `preventDefault()` on mousedown — [[Card Notes]]
- [doc] Noted that browser testing always uses "Test board", never the real training boards (CLAUDE.md)
- [fix] Clicking a block's last word — or the slack right of a short line — opened the block *below*. Resolving by "which block contains this offset" is ambiguous at a boundary; match on the block's start offset instead — [[Card Notes]]
- [fix] Blocks made only of zero-width characters (lone `U+200C` paragraphs from Confluence exports) rendered as blank, undeletable rows. `hasVisibleContent()` skips them like `space` tokens — [[Card Notes]]
- [fix] Every save stacked another blank line into the gap after the edited block — marked keeps blank lines in separate `space` tokens, so a block's `raw` doesn't own its separator. `blockSeparator()` keeps what's there and asks the lexer whether it still splits — [[Card Notes]]
- [decision] Verified the splice logic headlessly against the real Drops note before touching the browser: no-op edits round-trip byte-identically, 20 consecutive saves don't grow the source, emptying any block deletes exactly it, and the save delta predicts the next block's new start. That's what surfaced the newline accumulation and the heading-swallows-next-line case — neither was visible on screen
- [fix] Link chips stopped opening — `preventDefault()` on mousedown kills anchor navigation too. Mousedown inside an `<a>` now bails before it; the block is still editable from the slack beside the chip — [[Card Notes]]
- [decision] Archived rather than deleted: `pages/archive/PROJECT_HANDOFF.md` and `pages/archive/UX Batches 2026-07.md` (renamed from `UX Backlog`, 30 links rewritten). Gutting them would have destroyed the record of what was decided against; the batch record is history, not a work list — [[UX Batches 2026-07]]
- [decision] Six code findings from the [[Board View]] read filed as *unconfirmed* in [[Open Work]] rather than written into the wiki as fact — including a suspected off-by-one when dragging a card upwards in its own column, reproduced only in a scratch replay of the store logic. A wiki that states unverified findings as fact is worse than one that omits them
- [decision] **Journals are `[decision]` entries only from today.** Considered and rejected: folding the journals into the pages entirely for a single source of truth. Measured first — 75 of 80 entries already only pointed at a page, and the 50 `[fix]`/`[feature]` lines were a third copy of what the commit message and the page both held. But merging them in would append dated changelogs to feature pages, which is what turns a page into a diary ([[Card Notes]] was already showing it). Git is the changelog; the pages are the state; the journal keeps only what neither can hold — the alternative that was rejected and what it would have cost. See `CLAUDE.md`
- [decision] Harvested the two decisions that existed *only* in a journal onto [[Card Notes]]: the CodeMirror 6 live-preview rejection (~1–2 weeks, weakest on touch) and the headless splice verification. Both had journal lines linking to a page that never absorbed them — the rot the new rule targets
