- Reorganizing Juki Practice
- DONE Import function currently breaks column settings as it uses Trello style JSON
	- Fixed: export now uses the app's own full-fidelity JSON format ([[Tables]] — "Board import/export"); overwrite-import preserves existing column settings by name when the source file is a real Trello export
-