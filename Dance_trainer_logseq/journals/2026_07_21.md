- [feature] Card modal description field: deliberate typography (14px/1.5, styled headings/inline code/link chips), 280px scroll cap, hairline divider before Delete — [[Card Notes]]
- [feature] Description edit no longer jumps: textarea adopts the preview's measured height, and the caret lands in the block that was clicked (via `marked.lexer()` offsets) — [[Card Notes]]
- [feature] Description saves on blur; removed the Cancel button, which blur-save made unhonourable — [[Card Notes]]
- [fix] Board-face card links are no longer clickable — clicking one opens the card instead of navigating away mid-scan — [[Card Notes]]
- [fix] `textarea { }` in CardModal was a bare element selector styling every textarea in the file; scoped to `.desc-textarea` — [[Card Notes]]
- [doc] New [[Card Notes]] page covering the markdown pipeline and the description editing model
- [doc] UX critique across board / [[Practice Companion View]] / card modal — scored 17/40; findings filed as [[UX Backlog]], nothing implemented yet
- [decision] Corrected the [[Card Notes]] link-chip colour rationale: `--color-ember` is itself 3.30:1 as 10px text, so the choice picked the less-bad of two failing options — needs a new `--color-ember-text` token
- [fix] Practice overlays (add drawer, review) no longer survive a switch back to Board — mode buttons go through `setViewMode()` — [[UX Backlog]] [[Practice Companion View]]
- [fix] Removed `.review-backdrop`: an opaque full-screen `.review` sat above it, so it was invisible and its click-to-dismiss could never fire — [[Practice Companion View]]
- [fix] Topbar tightened at ≤760px and board name hidden in Practice mode, where it collapsed to a useless 40px fragment — [[UX Backlog]]
- [fix] `.desc-preview` base colour is now `--color-ink` with `.placeholder` as the dim exception; blockquotes/tables/`<pre>` were inheriting a 3.92:1 colour — [[Card Notes]]
- [fix] `.card-list` in the practice add drawer got the app's thin token scrollbar — [[Practice Companion View]]
- [fix] Add-drawer Added panel is always rendered at a fixed height instead of appearing on first add and growing — it was shoving the browse list down 51px per tap — [[Practice Companion View]] [[UX Backlog]]
- [fix] Add-drawer browse list is a `TransitionGroup`: the added row fades out of flow and the rows below slide up over 180ms, so the unavoidable 48px close-up is trackable rather than a teleport — [[Practice Companion View]]
- [decision] Dropped the `is_due_column` fallback-warning item — the board has a Due column flagged, so the leftmost-column fallback never fires — [[UX Backlog]]
