- [feature] Card modal description field: deliberate typography (14px/1.5, styled headings/inline code/link chips), 280px scroll cap, hairline divider before Delete — [[Card Notes]]
- [feature] Description edit no longer jumps: textarea adopts the preview's measured height, and the caret lands in the block that was clicked (via `marked.lexer()` offsets) — [[Card Notes]]
- [feature] Description saves on blur; removed the Cancel button, which blur-save made unhonourable — [[Card Notes]]
- [fix] Board-face card links are no longer clickable — clicking one opens the card instead of navigating away mid-scan — [[Card Notes]]
- [fix] `textarea { }` in CardModal was a bare element selector styling every textarea in the file; scoped to `.desc-textarea` — [[Card Notes]]
- [doc] New [[Card Notes]] page covering the markdown pipeline and the description editing model
- [doc] UX critique across board / [[Practice Companion View]] / card modal — scored 17/40; findings filed as [[UX Backlog]], nothing implemented yet
- [decision] Corrected the [[Card Notes]] link-chip colour rationale: `--color-ember` is itself 3.30:1 as 10px text, so the choice picked the less-bad of two failing options — needs a new `--color-ember-text` token
- [fix] Practice overlays (add drawer, review) no longer survive a switch back to Board — mode buttons go through `setViewMode()` — [[UX Backlog]] [[Practice Companion View]]
- [fix] Removed `.review-backdrop`: an opaque full-screen `.review` sat above it, so it was invisible and its click-to-dismiss could never fire — [[Practice Companion View]]
- [fix] Topbar tightened at ≤760px and board name hidden in Practice mode, where it collapsed to a useless 40px fragment — [[UX Backlog]]
- [fix] `.desc-preview` base colour is now `--color-ink` with `.placeholder` as the dim exception; blockquotes/tables/`<pre>` were inheriting a 3.92:1 colour — [[Card Notes]]
- [fix] `.card-list` in the practice add drawer got the app's thin token scrollbar — [[Practice Companion View]]
- [fix] Add-drawer Added panel is always rendered at a fixed height instead of appearing on first add and growing — it was shoving the browse list down 51px per tap — [[Practice Companion View]] [[UX Backlog]]
- [fix] Add-drawer browse list is a `TransitionGroup`: the added row fades out of flow and the rows below slide up over 180ms, so the unavoidable 48px close-up is trackable rather than a teleport — [[Practice Companion View]]
- [decision] Dropped the `is_due_column` fallback-warning item — the board has a Due column flagged, so the leftmost-column fallback never fires — [[UX Backlog]]
- [decision] Practice surface is a **continuous work model** — no day boundary, no expiry. Two states: being trained, and trained-awaiting-sort. Container renamed "practice queue"; the done group gets a "TO SORT (n)" header. Kills the critique's midnight-expiry proposal — [[Practice Companion View]] [[UX Backlog]]
- [decision] **No auto-seeding of the practice queue.** The critique's headline P0 ("Start with today's due cards" in one tap) was rejected outright; cards are chosen by what you'll physically drill. This makes the add drawer the priority surface, not the empty state — [[UX Backlog]]
- [decision] "Keep in Due" will stamp `due_date = today` in `resolve()`. It skipped `moveCard` entirely, so a practised-and-kept card never had its due date touched and accumulated lateness forever — found reading `boardStore.moveCard`, not in the critique — [[Practice Companion View]]
- [decision] Add drawer gets `dueLabel()` text, board-wide search, most-overdue-first tab sort, and arrow-key selection with Enter-adds-and-clears. The board's own search gets the same highlight/arrows — [[Practice Companion View]]
- [decision] Keep the Leitner box, fix the docs — SM2 declined because the algorithm choosing the interval would replace the manual column choice that Session Review exists for. `PRODUCT.md` and `CLAUDE.md` corrected; [[PROJECT_HANDOFF]] marked as a superseded spec rather than gutted
- [decision] Store `cards.last_scheduled_column_id` + `last_practiced_on` rather than deriving from `due_date − due_offset_days` — the derivation fails for exactly the cards in Review, which have all been swept into a `null`-offset column — [[Tables]]
- [decision] Declined for a single-user touch-first tool: Review undo/toast, bucket interval labels, bucket cap, focusable cards/column titles, focus traps, larger touch targets, darker hairlines. The border item's own proposed value (1.41) fails the 3.0 it demands — [[UX Backlog]]
- [decision] Reorder: the `⠿` becomes a real drag handle (immediate lift) with long-press-anywhere retained, plus FLIP motion so rows are seen making room instead of teleporting — [[Practice Companion View]]
- [doc] Critique accuracy audit — it measured *Test board* not the real one (14d late, not 56d), wrongly named `CLAUDE.md` as promising SM2, reported one label-chip ratio where there are six (three pass, three fail), and proposed a border fix that fails its own threshold — [[UX Backlog]]
- [doc] Rewrote [[UX Backlog]] around the agreed eight-batch plan, with a "Decided against" section and a "Corrections to the critique" section; raw findings kept for provenance
- [feature] Practice queue vocabulary landed: "Practice queue (n)", a labelled "To sort (n)" group header replacing the bare hairline, and the seven "today's session" strings gone — [[Practice Companion View]] [[UX Backlog]]
- [feature] Empty practice queue reports the board's situation ("13 cards due, 13 overdue") above one button. No seeding, by decision — [[Practice Companion View]]
- [feature] View mode persists to `practice-view-mode:{boardId}`; a reload mid-practice no longer drops you on the Kanban board — [[Practice Companion View]]
- [feature] Add drawer: `dueLabel()` text on every row, board-wide search that hides the tabs while querying, most-overdue-first tab sort with undated last, and ↑/↓/Enter selection where Enter adds and clears so the next name can be typed — [[Practice Companion View]] [[UX Backlog]]
- [fix] Board search Enter fired on a hardcoded `searchResults[0]`; it now respects the arrow-key highlight — [[UX Backlog]]
- [fix] "Keep in Due" never rescheduled — `resolve(null)` skipped `moveCard`, so a practised-and-kept card's due date was never touched and its lateness climbed forever. New `recordPracticeInPlace()` — [[Practice Companion View]] [[Tables]]
- [decision] "Keep in place" applies the card's *own* column's `due_offset_days` rather than always stamping today. Reduces to the agreed behaviour for the Due column (offset `null`) but stops a card kept in Wöchentlich from being made due immediately. Button now names the actual column — [[UX Backlog]]
- [fix] Review completion copy counts real moves ("3 rescheduled · 2 kept in place") instead of reporting the queue length as cards sorted — [[Practice Companion View]]
- [feature] Migrated `cards.last_scheduled_column_id` + `last_practiced_on`, written when a column's on-enter rule fires and by `recordPracticeInPlace()`. Surfaced as "Wöchentlich · practiced 8d ago" on the focus card (so Session Review gets it free) and in the card modal — [[Tables]]
- [decision] `boardFormat.ts` round-trips both history fields: the export is offered as a backup, so dropping practice history on restore is silent data loss — [[Tables]]
- [feature] Topbar decluttered — invite code, Copy/Export/Import, the signed-in address and Sign out now live only in the `⋯` menu, which is what finally un-truncates the board name — [[UX Backlog]]
- [feature] Reorder handle is real: pressing `⠿` lifts the row immediately (30×44 target, readable colour), long-press-anywhere retained, and the other rows FLIP into place instead of teleporting — [[Practice Companion View]]
- [fix] Done FAB reserves a gutter so notes no longer run under it, and its receipt moved off the thumb into a "✓ Logged" pill above the button, held 900ms instead of 200ms — [[Practice Companion View]]
- [fix] Session rows' two 7px dots differentiated: urgency is now a 3×18px bar echoing the board card's status stripe, label colour stays a circle — [[UX Backlog]]
- [feature] Contrast pass: `--color-ink-dim` → `#6a6156` (5.11/4.62), new `--color-ember-text` `#a04c00` for ember-as-text and the focus ring, per-label chip text colours via `LABEL_TEXT_COLORS` — [[UX Backlog]]
- [decision] Label `sage` lightened `#6e8b6b` → `#7d9a7a`: it was the one fill no text colour could clear (3.77 white / 4.06 ink); ink now reaches 4.94. Changes how existing sage labels look — [[UX Backlog]]
- [fix] `.md-link-chip` head-truncates so the distinguishing tail shows — and it was `inline-flex`, where `text-overflow` doesn't apply, so it had been hard-clipping with no ellipsis at all — [[Card Notes]]
- [fix] Markdown heading override consolidated into one em-relative block in `tokens.css` for all three surfaces; an `<h1>` in a description no longer renders at 24px on a card face — [[Card Notes]]
- [fix] `PracticeFocusCard`'s `.note-preview` had the same inverted colour base `.desc-preview` had — headings rendered dimmer than the body under them — [[Card Notes]]
- [fix] Caret lands in the tapped block when editing a note in the practice view; straight port of `CardModal.startEditDescription()` — [[Card Notes]]
- [feature] Toasts: error/success variants, manual dismiss, 10s hold for errors, raw Supabase strings demoted below a headline, `role="status"` on the stack — [[UX Backlog]]
- [fix] Board-face markdown strips `href`, removing ~100 focusable invisible anchors from the tab order. `renderMarkdownWithLinkChips()` now runs its own pass, since it was built on `renderMarkdown()` and would have lost its links too — [[Card Notes]]
- [doc] All eight [[UX Backlog]] batches shipped as v0.7.0. Recorded deviations, defects found during implementation, and what shipped short of plan (no toast retry actions, generic error copy). Nothing browser-verified — that session had no browser tooling
- [fix] FLIP was silently dead. `.list-row.dragging { transition: none; transform: scale() }` outranked `.row-move`, and TransitionGroup decides whether to animate at all by cloning the *first* previous child and checking whether the move class resolves to a transform — so lifting the top row disabled the motion for every row. The lift is colour and shadow only now. Found by browser-testing, not by review — [[Practice Companion View]]
- [fix] "Keep in place" was enrolling undated cards into spaced repetition. A card parked in Library und Backlog with no due date got stamped today and would have been swept into Today the next day. It now keeps its due date null when the column has no rule and the card had no date; the practice date is still recorded — [[Tables]]
- [doc] Corrected this page's own claim that the topbar declutter un-truncates the board name: measured at 386px, `.board-name` still gets 62px for an 85px name. The invite chrome was already hidden under the ≤760px media query, so the win is desktop-only — [[UX Backlog]]
- [fix] Renamed the card-history migration `20260721000000` → `20260721143750` to match the version the development apply recorded remotely. `supabase db push` refuses a local file sorting before the last applied migration, so the round stamp would have failed the deploy on this push and every one after — [[Supabase]] [[Tables]]
- [fix] Note editing now drops the caret on the word you clicked, not at the start of its block. `caretOffsetFromClick()` maps a click through `caretPositionFromPoint()` → lexer block index → a whitespace-ignoring greedy walk of rendered text against raw source, so `**`, `# ` and `- ` are stepped over. Block-level mapping had also fallen back to end-of-source for any click not squarely on a block child — the gap between two paragraphs included — which is why the field still read as "jumps to the end" after the earlier port. One shared function, used by both [[Card Notes]] surfaces
