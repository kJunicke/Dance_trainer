- [decision] Reversed the 2026-07-21 decision against a shared markdown-note component: the three surfaces' differing type sizes and palettes are *values*, and CSS custom properties carry values across a component boundary without props — the "props explosion, earns nothing" reasoning that killed the idea originally didn't hold. `MarkdownNote.vue` is now that component, themed per-surface via `--note-*` vars — [[Card Notes]]
- [decision] Adjacent same-marker lists merge into one editable block, accepted as-is — CommonMark dropped the "blank line ends a list" rule, so no separator `blockSeparator()` returns can keep them apart. Rejected: injecting `<!-- -->` comments, the one construct that would actually separate them, because it writes permanent noise into notes kept for years to fix a case where no bytes are lost — [[Card Notes]]
- [decision] `list_item`s again not split into separately-editable blocks. Reaffirmed, not revisited — the existing [[Open Work]] entry stands unchanged — [[Card Notes]]
- [decision] No test suite added for this pass either; verified with a throwaway property script against a real note rather than committing tests, same as the 2026-07-25 prototype — [[Card Notes]]
- [decision] Enter on an *empty* list item clears the marker outright, ending the list, rather than outdenting one level per press the way desktop editors do. Stepwise outdent is what you want when nesting is deep, but on a phone the bar's `⇤` already does exactly that, and the escape hatch has to be one press or it isn't an escape hatch — [[Card Notes]]
- [decision] The card title must be the largest thing on a board card, enforced by flattening in-note headings to the 12px base (`--note-heading-scale: 0`) rather than by raising `.task-title` above them. Rejected the title bump: it's the same fix pointed the other way, but it costs column density on a phone, where how many cards fit above the fold is the point of the face — [[Card Notes]]
- [decision] Mobile board swipe distance left as-is. Rejected `scroll-snap-stop: always`, the one-line fix that makes it perfectly repeatable, because it caps every gesture at one column and `JuKi practise` has 14 of them. CSS has no strength dial between the two — the JS fling that would give both is deferred to [[Open Work]] — [[Board View]]
- [decision] Swiping a practice-queue row removes the card from the *session*, not from the board. Rejected a real card delete there (and a two-action row offering both): mid-practice the thumb is one width away from a mis-swipe, and a card carries months of notes and schedule that nothing else in the app can restore — [[Practice Companion View]]
- [decision] Quick add is its own panel, not an extension of the add drawer or of the board's header search. Rejected reusing the board search with a mode branch — its hit opens the card modal, and the two surfaces answer different questions ("build me a queue" vs "queue this one skill I just thought of") — [[Practice Companion View]]
- [decision] `is_inbox_column` has no fallback column and no partial unique index. Rejected defaulting to the due column when none is set (a created card would silently enter the schedule) and rejected mirroring `is_due_column`'s unique index (single-inbox is a store invariant; two set is harmless, `inboxColumn` takes the first) — [[Tables]]
- [decision] Retired the `dance-trainer-design-system` skill in favour of a root `DESIGN.md` (generated by `/impeccable document`, sidecar at `.impeccable/design.json`). It was still accurate, but ~40% of it restated `PRODUCT.md` — two files asserting the same brand rules drift, and the skill had already drifted from `tokens.css` once. Rejected the cheaper option of trimming the skill down to a pure token reference and keeping it: it kept the auto-load-on-design-work advantage, but left three homes for visual knowledge instead of two, and `DESIGN.md` is the format the design tooling already reads — [[contents]] Code map
- [decision] Card relations are a navigation and authoring structure only — they never feed the practice queue. Rejected surfacing a family as a contiguous run: that is blocked practice by definition (Shea & Morgan 1979), right while a part is raw and wrong once it's stable, and an affordance would make the wrong one permanent and one-tap. Same argument that killed auto-seeding the queue on 2026-07-21; note the evidence is contested in applied settings (applied d≈0.19 vs lab d≈0.57), so it's "not the default", not a ban — [[Card Relationships]]
- [decision] Cards store the **leaf name only**, with the ancestor chain derived at render time. Rejected storing the composed `"OFS - Prep"` string: renaming a parent would need a recursive cascade-rewrite of every descendant, and would silently overwrite any child name the user had hand-edited. The cost is that board search has to match the composed chain rather than the stored column — [[Card Relationships]]
- [decision] Merge stays cheap: one dialog, no interval guard and no practice-history line folded into the note. Rejected warning when a daily part folds into a monthly parent (pure arithmetic on `due_offset_days`, one line in a dialog already being built) — the user knows their own columns, and merge is the step that gets skipped, which is the whole risk over-decomposition creates (Masters 1992). Also rejected writing the dead card's `last_practiced_on` into the note: notes stay hand-written, consistent with the `<!-- -->` rejection above — [[Card Relationships]]
- [decision] No proactive "time to merge?" prompt, though the research offers a clean trigger (all parts at or above the parent's Leitner level). Declined as a new kind of voice the app doesn't have — [[Card Relationships]]
- [decision] Relations are created from pickers in the card modal, not by dragging one card onto another. Rejected drop-to-parent: the board runs two drag systems (native + hand-rolled touch) that hit-test for drop-*between*, and disambiguating "move here" from "make this its parent" mid-gesture is high risk against working code — [[Card Relationships]]
- [decision] The board-face family icon opens the card's own modal scrolled to its Family section. Rejected a popover (a floating layer anchored to a board that scrolls horizontally under it) and inline card expansion (changes card height while the touch drag hit-tests card rectangles) — [[Board View]]
- [decision] Split lands the new card in the **inbox column**, falling back to the parent's column when no inbox is set. Reverses the same-day decision to always use the parent's column — that one was made only because `is_inbox_column` didn't exist yet and relations had to stay self-contained; it shipped for practice quick-add hours later, removing the reason. Rejected keeping the parent's column now that there's a choice: it enrols a freshly cut part at the parent's interval, and a part raw enough to need splitting out is exactly the one not yet worth reviewing on the parent's schedule. Accepted cost — an inbox with no offset leaves the part undated, i.e. out of spaced repetition until Session Review triages it — [[Card Relationships]]
- [decision] The split action lives in `CardModal`, never in `MarkdownNote`. Rejected a per-block split button: `MarkdownNote` is shared with the board face and the practice focus card, and would hand both a card-mutating control they must not have — [[Card Notes]]
- [decision] Session Review keeps the card's note **full-length and editable** inside a three-zone layout (pinned head / scrolling note / pinned bucket tray). Rejected collapsing the note behind a `show note` toggle, which fits most cards on one screen with no scrolling at all: notes get *written* during review, not only read, so a toggle would be tapped on every card that matters. Also rejected dropping the note from Review entirely — [[Practice Companion View]]
- [decision] Session Review's bucket tray hides while a note block is being edited, via an additive `update:editing` emit on the shared `MarkdownNote`. Rejected stacking the tray under the component's existing fixed formatting bar, which needs no change to a component serving three surfaces: two fixed bars above the on-screen keyboard leave ~300px of a 390×844 screen for the note you're writing, and six formatting glyphs sitting on five interval buttons reads as one broken control — [[Practice Companion View]]
- [decision] Swipe-to-remove stays `--pc-overdue` red, and gains swipe-right-to-close instead. Rejected dimming it to neutral to fix the inverted risk gradient (the reversible action shouts, the irreversible bucket tap whispers): the reveal-then-tap gesture is already the confirmation step, and a quiet affordance invites the mis-swipe the two-step gesture exists to prevent. The gradient is corrected from the other end — Session Review's pinned head and visible intervals — [[Practice Companion View]]
- [decision] The practice palette gets one focus-ring re-toning (`--pc-focus`), not a second focus style. Rejected leaving the global `--color-ember-text` ring in place: it is 3.04–3.21:1 on the near-black grounds, clearing the non-text floor but reading darker than the ember border a selected row already carries, so focusing a row made it look *less* selected than the active one. Three components had already patched it locally, which is the drift the single-ring rule exists to prevent — [[Practice Companion View]], DESIGN.md
- [decision] Deferred every board-face treatment for card lineage after prototyping six of them at true column width. Rejected outright — not deferred — the family key (a colour hashed from the parent name), which is the only pre-attentive option and would have been the strongest fix, because the face already encodes urgency in a coloured status rail and user meaning in coloured label chips, and a third colour language is how all three stop meaning anything. Also rejected the tree-elbow outline (parts are scattered by schedule, so the connector draws a hierarchy that isn't on screen) and the mono ember mark (spends the action colour on a purely informational string). The practice surfaces were unblocked from this and shipped their own treatment — [[Open Work]], [[Card Relationships]]
- [decision] Practice surfaces show the lineage in two registers, not one: display-face title line on the focus card and Session Review, 10px mono sub-line in the queue rows, add drawer and quick add. Rejected using the title treatment everywhere — a parented queue row costs ~13px, about one row off a 34dvh five-row list, which is affordable once and not three times — [[Card Relationships]]
- [decision] Markdown heading ramp widened from 1.15/1.08em to 1.5/1.25/1.05em, with space above and a hairline under h1. Rejected keeping size as the only channel: at the modal's 14px base the old steps put h1 at 16.1px against a 15.1px h2 and a 14px h3, a 1.07× ratio that isn't a hierarchy, and every editor worth copying runs ~1.2–1.3× *and* pairs size with weight, space or a rule. The `--note-heading-scale` mechanism was kept rather than replaced, so the board face still flattens to a single size — [[Card Notes]]
- [decision] The whole-view scroll was mixed viewport units, not a practice-view bug: `body` was `min-height: 100vh` (the *large* viewport) inside a `100dvh` board, and `--session-list-height` was `34vh` inside the same. Fixed by moving every practice-surface `vh` to `dvh`; deliberately left `LoginView`'s `100vh` alone, since a login page may legitimately scroll — [[Practice Companion View]]
- [decision] The practice surfaces name a card's parents in **two registers**, not one: the focus card and Session Review's head take the lineage as the title's first line (display face, 13px/600), the queue rows, add drawer and quick add take a 10px mono sub-line. Rejected one uniform treatment — a list row isn't a card object, and the register that reads as "the title continues below" on a full-screen card reads as a second title in a 44px row. Also rejected leaving the walk in `BoardView` and re-deriving it per practice component: six consumers, and an ancestor walk that re-indexes the array per card is O(n²) per render — [[Card Relationships]]
- [decision] The practice focus card and Session Review's title went 20px → 22px rather than clamping the note's new h1 ramp. Rejected capping h1 below 20px: the ramp is shared across all three note surfaces and the board face already buys its exemption with `--note-heading-scale: 0`, so capping would have paid for one surface's spare room out of every surface's heading hierarchy — [[Card Notes]], DESIGN.md
- [decision] Deferred making a parted card look different from an unparted one on the **board face**; six treatments were prototyped in the real tokens at true column width and none accepted. Three were rejected on principle rather than taste: a colour-hashed family key overloads a face already spending colour on the status rail and label chips; a tree elbow draws a hierarchy that isn't there, since parts are scattered by schedule rather than filed under their parent; a mono mark in `--color-ember-text` spends the *action* colour on decoration. The finding kept: only the folder tab and the nameplate change the card's **silhouette**, and silhouette is what fixes scanning — the rest only fix reading — [[Open Work]]
- [decision] Deferred the quick-target column that swallows a practice event (no `due_offset_days`, no `due_clear_on_enter` — live on JuKi's `Today`). Called an edge case that doesn't bite in use; recorded rather than fixed because the two candidate fixes differ in kind — change `moveCard`'s shared scheduling code, or change the column's configuration — [[Open Work]]
