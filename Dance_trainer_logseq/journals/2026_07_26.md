- [decision] Reversed the 2026-07-21 decision against a shared markdown-note component: the three surfaces' differing type sizes and palettes are *values*, and CSS custom properties carry values across a component boundary without props — the "props explosion, earns nothing" reasoning that killed the idea originally didn't hold. `MarkdownNote.vue` is now that component, themed per-surface via `--note-*` vars — [[Card Notes]]
- [decision] Adjacent same-marker lists merge into one editable block, accepted as-is — CommonMark dropped the "blank line ends a list" rule, so no separator `blockSeparator()` returns can keep them apart. Rejected: injecting `<!-- -->` comments, the one construct that would actually separate them, because it writes permanent noise into notes kept for years to fix a case where no bytes are lost — [[Card Notes]]
- [decision] `list_item`s again not split into separately-editable blocks. Reaffirmed, not revisited — the existing [[Open Work]] entry stands unchanged — [[Card Notes]]
- [decision] No test suite added for this pass either; verified with a throwaway property script against a real note rather than committing tests, same as the 2026-07-25 prototype — [[Card Notes]]
- [decision] Enter on an *empty* list item clears the marker outright, ending the list, rather than outdenting one level per press the way desktop editors do. Stepwise outdent is what you want when nesting is deep, but on a phone the bar's `⇤` already does exactly that, and the escape hatch has to be one press or it isn't an escape hatch — [[Card Notes]]
- [decision] The card title must be the largest thing on a board card, enforced by flattening in-note headings to the 12px base (`--note-heading-scale: 0`) rather than by raising `.task-title` above them. Rejected the title bump: it's the same fix pointed the other way, but it costs column density on a phone, where how many cards fit above the fold is the point of the face — [[Card Notes]]
- [decision] Mobile board swipe distance left as-is. Rejected `scroll-snap-stop: always`, the one-line fix that makes it perfectly repeatable, because it caps every gesture at one column and `JuKi practise` has 14 of them. CSS has no strength dial between the two — the JS fling that would give both is deferred to [[Open Work]] — [[Board View]]
- [decision] Swiping a practice-queue row removes the card from the *session*, not from the board. Rejected a real card delete there (and a two-action row offering both): mid-practice the thumb is one width away from a mis-swipe, and a card carries months of notes and schedule that nothing else in the app can restore — [[Practice Companion View]]
- [decision] Quick add is its own panel, not an extension of the add drawer or of the board's header search. Rejected reusing the board search with a mode branch — its hit opens the card modal, and the two surfaces answer different questions ("build me a queue" vs "queue this one skill I just thought of") — [[Practice Companion View]]
- [decision] `is_inbox_column` has no fallback column and no partial unique index. Rejected defaulting to the due column when none is set (a created card would silently enter the schedule) and rejected mirroring `is_due_column`'s unique index (single-inbox is a store invariant; two set is harmless, `inboxColumn` takes the first) — [[Tables]]
- [decision] Retired the `dance-trainer-design-system` skill in favour of a root `DESIGN.md` (generated by `/impeccable document`, sidecar at `.impeccable/design.json`). It was still accurate, but ~40% of it restated `PRODUCT.md` — two files asserting the same brand rules drift, and the skill had already drifted from `tokens.css` once. Rejected the cheaper option of trimming the skill down to a pure token reference and keeping it: it kept the auto-load-on-design-work advantage, but left three homes for visual knowledge instead of two, and `DESIGN.md` is the format the design tooling already reads — [[contents]] Code map
- [decision] Card relations are a navigation and authoring structure only — they never feed the practice queue. Rejected surfacing a family as a contiguous run: that is blocked practice by definition (Shea & Morgan 1979), right while a part is raw and wrong once it's stable, and an affordance would make the wrong one permanent and one-tap. Same argument that killed auto-seeding the queue on 2026-07-21; note the evidence is contested in applied settings (applied d≈0.19 vs lab d≈0.57), so it's "not the default", not a ban — [[Card Relationships]]
- [decision] Cards store the **leaf name only**, with the ancestor chain derived at render time. Rejected storing the composed `"OFS - Prep"` string: renaming a parent would need a recursive cascade-rewrite of every descendant, and would silently overwrite any child name the user had hand-edited. The cost is that board search has to match the composed chain rather than the stored column — [[Card Relationships]]
- [decision] Merge stays cheap: one dialog, no interval guard and no practice-history line folded into the note. Rejected warning when a daily part folds into a monthly parent (pure arithmetic on `due_offset_days`, one line in a dialog already being built) — the user knows their own columns, and merge is the step that gets skipped, which is the whole risk over-decomposition creates (Masters 1992). Also rejected writing the dead card's `last_practiced_on` into the note: notes stay hand-written, consistent with the `<!-- -->` rejection above — [[Card Relationships]]
- [decision] No proactive "time to merge?" prompt, though the research offers a clean trigger (all parts at or above the parent's Leitner level). Declined as a new kind of voice the app doesn't have — [[Card Relationships]]
- [decision] Relations are created from pickers in the card modal, not by dragging one card onto another. Rejected drop-to-parent: the board runs two drag systems (native + hand-rolled touch) that hit-test for drop-*between*, and disambiguating "move here" from "make this its parent" mid-gesture is high risk against working code — [[Card Relationships]]
- [decision] The board-face family icon opens the card's own modal scrolled to its Family section. Rejected a popover (a floating layer anchored to a board that scrolls horizontally under it) and inline card expansion (changes card height while the touch drag hit-tests card rectangles) — [[Board View]]
- [decision] Split lands the new card in the **parent's column**, not in an inbox column. `is_inbox_column` was reversed mid-design to keep relations self-contained; it then shipped anyway for practice quick-add, but split still uses the parent's column — a split is a card arriving in a column, with no special scheduling path — [[Card Relationships]]
- [decision] The split action lives in `CardModal`, never in `MarkdownNote`. Rejected a per-block split button: `MarkdownNote` is shared with the board face and the practice focus card, and would hand both a card-mutating control they must not have — [[Card Notes]]
