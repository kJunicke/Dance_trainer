- [fix] Note editor opened scrolled to the note's *bottom* with the caret off-screen — `setSelectionRange` never scrolls the caret into view in Blink and `v-model` parks a mounted textarea at its end. New shared `focusAtOffset()` focuses with `preventScroll`, then centres the caret's line via an off-screen mirror (`scrollCaretIntoView`) — [[Card Notes]]
- [feature] Card modal widened 560px → 720px on desktop, preview cap 280px → 440px at ≥641px (phone sheet unchanged) — the notes field now uses the desktop's spare space instead of floating in a narrow column — [[Card Notes]]
